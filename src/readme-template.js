// const generateTitle = projectTitle => {
//     console.log(projectTitle);
// }

module.exports = templateData => {
    console.log(templateData);
    const {gitUser, email, projTitle} = templateData
    console.log(gitUser);
}